import createEmotion from '@emotion/css/create-instance'

export const {
  // flush,
  // hydrate,
  // cx,
  // merge,
  // getRegisteredStyles,
  // injectGlobal,
  // keyframes,
  css: cij,
  // sheet,
  // cache,
} = createEmotion({ key: 'schema-render' })
